{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "required": ["file"],
  "properties": {
    "file": {"type": "string", "description": "commOcr 结果库路径（.db/.sqlite）"},
    "long_value_chars": {"type": "integer", "minimum": 1, "description": "长值阈值，默认 200"}
  }
}
