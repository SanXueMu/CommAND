services:
  command-pg:
    image: postgres:16
    environment:
      POSTGRES_DB: command
      POSTGRES_USER: command
      POSTGRES_PASSWORD: command
    ports: ["5433:5432"]
    volumes: [command-pg:/var/lib/postgresql/data]
volumes:
  command-pg:
